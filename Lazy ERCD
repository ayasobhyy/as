#include <bits/stdc++.h>
using namespace std;
int main ()
{
    freopen("lazy.in","r",stdin);
    int n ,t;
    cin>>n;
    for(int i=1;i<=n;i++)
       {
            cin>>t;
            cout<<"Case"<<" "<<i<<":"<<" "<<t-1<<endl;
       }
return 0;
}
